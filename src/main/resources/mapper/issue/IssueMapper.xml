<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pj.sayyo.model.issue.mapper.IssueMapper">

<insert id="regist" parameterType="IssueDto">
        INSERT INTO ISSUE(ID, REGION, TITLE, URL, REPORTS)
        VALUES (#{id}, #{region}, #{title}, #{url}, 0)
    </insert>

    <select id="findAll" resultType="IssueDto">
        SELECT ID, REGION, TITLE, URL, REPORTS
        FROM ISSUE
    </select>

    <update id="modify" parameterType="IssueDto">
        UPDATE ISSUE
        SET TITLE = #{title}
        WHERE ID = #{id}
    </update>

    <update id="report" parameterType="IssueDto">
        UPDATE ISSUE
        SET REPORTS = REPORTS+1
        WHERE ID = #{id}
    </update>

    <delete id="delete" parameterType="IssueDto">
        DELETE ISSUE
        WHERE ID = #{id}
    </delete>

    <select id="findSearch" parameterType="IssueDto" resultType="IssueDto">
        SELECT (ID, REGION, TITLE, URL, REPORTS)
        FROM ISSUE
        <if test='keyword != null and keyword != ""'>
            WHERE ${searchCode} = #{keyword}
        </if>
    </select>
</mapper>