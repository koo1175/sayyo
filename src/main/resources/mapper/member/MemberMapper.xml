<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pj.sayyo.model.member.mapper.MemberMapper">

    <insert id="regist" parameterType="MemberDto">
        INSERT INTO MEMBER(ID, PW, NAME, NICKNAME, PHONE, ADDRESS, REGIST_NUM, REPORTS)
        VALUES (#{id,jdbcType=VARCHAR}, #{pw,jdbcType=VARCHAR}, #{name,jdbcType=VARCHAR},
                #{nickname,jdbcType=VARCHAR}, #{phone,jdbcType=VARCHAR}, #{address,jdbcType=VARCHAR}, #{registNum,jdbcType=VARCHAR}, #{reports,jdbcType=INTEGER})
    </insert>

    <select id="findAll"
            resultType="MemberDto">
        SELECT ID, PW, NAME, NICKNAME, PHONE, ADDRESS, REGIST_NUM, REPORTS
        FROM MEMBER
    </select>

    <select id="login" parameterType="MemberDto" resultType="MemberDto">
        SELECT ID, PW, NAME, NICKNAME, PHONE, ADDRESS, REGIST_NUM, REPORTS
        FROM MEMBER
        WHERE ID = #{id} AND PW = #{pw}
    </select>

    <update id="kakao" parameterType="MemberDto">
        UPDATE MEMBER
        SET NICKNAME = #{nickname}, PHONE = #{phone}, ADDRESS = #{address}, REGIST_NUM = #{registNum}
        WHERE ID = #{id}
    </update>

    <update id="modify" parameterType="MemberDto">
        UPDATE MEMBER
        SET PW = #{pw}, ADDRESS = #{address}
        WHERE ID = #{id}
    </update>

    <update id="report" parameterType="MemberDto">
        UPDATE MEMBER
        SET REPORTS = REPORTS+1
        WHERE NICKNAME = #{nickname}
    </update>

    <delete id="delete" parameterType="MemberDto">
        DELETE MEMBER
        WHERE ID = #{id}
    </delete>

    <select id="findSearch" parameterType="MemberDto" resultType="MemberDto">
        SELECT ID, PW, NAME, NICKNAME, PHONE, ADDRESS, REGIST_NUM, REPORTS
        FROM MEMBER
        WHERE NICKNAME = #{nickname}
    </select>

    <select id="findByUserId" parameterType="MemberDto" resultType="MemberDto">
        SELECT * FROM MEMBER WHERE ID = #{id}
    </select>

</mapper>