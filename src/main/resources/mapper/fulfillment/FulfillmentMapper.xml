<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pj.sayyo.model.fulfillment.mapper.FulfillmentMapper">

    <insert id="regist" parameterType="FulfillmentDto">
        INSERT INTO FULFILLMENT(POLITICIAN_NUM, PLAN_NUM, COMPLETE_NUM, TRY_NUM, REGION_DEV)
        VALUES (#{politicianNum}, #{planNum},
                #{completeNum}, #{tryNum}, #{regionDev})
    </insert>

    <select id="findAll"
            resultType="FulfillmentDto">
        SELECT POLITICIAN_NUM, PLAN_NUM, COMPLETE_NUM, TRY_NUM, REGION_DEV
        FROM FULFILLMENT
    </select>

    <update id="modify" parameterType="FulfillmentDto">
        UPDATE FULFILLMENT
        SET PLAN_NUM = #{planNum}, COMPLETE_NUM = #{completeNum}, TRY_NUM = ${tryNum}, REGION_DEV = #{regionDev}
        WHERE POLITICIAN_NUM = #{politicianNum}
    </update>

    <delete id="delete" parameterType="FulfillmentDto">
        DELETE FULFILLMENT
        WHERE POLITICIAN_NUM = #{politicianNum}
    </delete>

    <select id="findSearch" parameterType="FulfillmentDto" resultType="FulfillmentDto">
        SELECT (POLITICIAN_NUM, PLAN_NUM, COMPLETE_NUM, TRY_NUM, REGION_DEV)
        FROM FULFILLMENT
        <if test='keyword != null and keyword != ""'>
            WHERE ${searchCode} = #{keyword}
        </if>
    </select>

</mapper>